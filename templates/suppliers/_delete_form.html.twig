<form method="post" action="{{ path('app_suppliers_delete', {'id': supplier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ supplier.id) }}">
    <button class="fs-button del-btn">Delete</button>
</form>
